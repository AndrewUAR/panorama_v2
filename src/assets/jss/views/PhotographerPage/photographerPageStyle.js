const photographerPageStyle = (theme) => ({
  container: {
    minHeight: '100vh',
    height: 'auto',
    width: '100vw',
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'column',
    marginBottom: '2rem'
  },
  buttonGroup: {
    margin: '2rem 0'
  }
});

export default photographerPageStyle;
